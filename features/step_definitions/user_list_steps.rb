#encoding: utf-8

Given(/^"(.*?)" 라는 이름의 사용자가 있을 때$/) do |username|
  #pending # express the regexp above with the code you wish you had
  #@user = User.create(name: username)
end

When(/^사용자 목록 페이지에 방문하면$/) do
  #pending # express the regexp above with the code you wish you had
  #visit users_path
end

Then(/^"(.*?)" 사용자가 목록에 출력 되는 것$/) do |username|
  #pending # express the regexp above with the code you wish you had
  #response.with username
end
